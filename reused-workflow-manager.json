{
    "enabledManagers": [
        "regex"
    ],
    "regexManagers": [
        {
            "fileMatch": [
                "^.github/workflows/.+.yaml$"
            ],
            "matchStrings": [
                "uses: (?<depName>.+?\/.+?)\/.+.yaml@(?<currentValue>.+)"
            ],
            "datasourceTemplate": "github-tags"
        }
    ]
}
